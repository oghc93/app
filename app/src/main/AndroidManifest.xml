<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.CAMERA" /> <!-- untuk scan QR config, opsional -->

    <application
        android:name=".XrayLiteApp"
        android:allowBackup="false"
        android:label="XrayLite Client"
        android:icon="@drawable/ic_launcher"
        android:theme="@style/Theme.XrayLite"
        android:usesCleartextTraffic="false">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Terima config link vless:// vmess:// trojan:// dari share/browser -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="vless" />
                <data android:scheme="vmess" />
                <data android:scheme="trojan" />
            </intent-filter>
        </activity>

        <service
            android:name=".vpn.XrayVpnService"
            android:permission="android.permission.BIND_VPN_SERVICE"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <intent-filter>
                <action android:name="android.net.VpnService" />
            </intent-filter>
            <!-- Wajib ada sejak Android 14 (API 34) untuk foregroundServiceType="specialUse",
                 kalau tidak ada startForeground() akan crash MissingForegroundServiceTypeException
                 begitu app dijalankan di device Android 14+ (targetSdk project ini = 34). -->
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="vpn-tunnel-client" />
        </service>

    </application>
</manifest>
